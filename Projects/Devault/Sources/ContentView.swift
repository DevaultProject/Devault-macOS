import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Devault")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .frame(width: 400, height: 300)
    }
}
